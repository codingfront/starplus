import styled from "styled-components";

export const GenreSidebarStyle = styled.div`
  .ant-list-grid {
    .genre-sidebar-item {
      margin: 0;
      padding: 0;
      margin-block-end: 0;
      a {
        display: block;
        padding: 10px 0;
        border-bottom: 3px solid rgba(255, 255, 255, 0.1);
        font-size: 15px;
      }
    }
  }
`;
